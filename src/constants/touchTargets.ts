/**
 * Touch Target Constants
 * Minimum touch target sizes for mobile accessibility (WCAG 2.1)
 * Minimum: 44x44px (iOS) / 48x48dp (Android)
 */

export const TOUCH_TARGET_MIN_SIZE = 44; // pixels
export const TOUCH_TARGET_MIN_HEIGHT = 44;
export const TOUCH_TARGET_MIN_WIDTH = 44;
export const TOUCH_TARGET_PADDING = 8; // Minimum spacing between touch targets

// Button styles
export const BUTTON_MIN_HEIGHT = TOUCH_TARGET_MIN_HEIGHT;
export const BUTTON_PADDING_VERTICAL = 12;
export const BUTTON_PADDING_HORIZONTAL = 16;

// Chip styles
export const CHIP_MIN_HEIGHT = TOUCH_TARGET_MIN_HEIGHT;
export const CHIP_PADDING_VERTICAL = 8;
export const CHIP_PADDING_HORIZONTAL = 12;

// Icon button styles
export const ICON_BUTTON_SIZE = TOUCH_TARGET_MIN_SIZE;
export const ICON_BUTTON_PADDING = 12;

// List item styles
export const LIST_ITEM_MIN_HEIGHT = TOUCH_TARGET_MIN_HEIGHT;
export const LIST_ITEM_PADDING_VERTICAL = 12;
export const LIST_ITEM_PADDING_HORIZONTAL = 16;

